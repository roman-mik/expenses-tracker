// Placeholder shared component, standing up the @kapa/ui workspace package
// so apps/web can prove it resolves through the monorepo. Real components
// migrate here once the shared UI direction (e.g. Tamagui) is decided —
// see PLAN.md.
export function Placeholder() {
  return 'kapa-ui-placeholder';
}
